# Handoff: lap 147 — §5.2 WIRED to Buchholz §14.25 (major-premise dispatch); no DROP, a sharpening

**Date**: 2026-06-26 · **Branch**: plan · build 🟢 green (1326 jobs, `lake build GoodsteinPA`).
Headline footprint UNCHANGED (`peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]`
— 0 math axioms). ONE commit (major-premise dispatch wiring).

Operator SOLE-OBJECTIVE this run = **M1b-term** (resolve crux-2 termination). The live
`false_of_ZDerivesEmpty` path rests on TWO sorries: `descent_step_K_noncritical` (Buchholz §5.2) and
(A) `exists_sigma1_descending_step` (gDef). This lap attacked the hardest-first one: §5.2.

## What this lap DID — wire the major-premise machinery into `descent_step_K_noncritical`
`descent_step_K_noncritical` (`Crux2Blueprint:2190`) is now a **sorry-FREE dispatcher** on the FAITHFUL
major premise (first `⊥`-exit, `majorIdx`), REPLACING the lap-129 stall-prone `permIdx` framing. The
banked-but-unused lap-129/130 machinery (`majorPrem_tag_mem`, `majorPrem_zAxAll_cutPartner`,
`majorPrem_zAxNeg_cutPartner`, `majorIdx_botOrbit_reducible`) is finally WIRED into the live path. Via
`majorPrem_tag_mem` (major premise tag ∈ {3,4,5,6}) it routes to two named sub-sorries aligned with
Buchholz §14.25:
- **`descent_step_K_noncrit_repMajor`** (`Crux2Blueprint:2163`, tag 3/4, §14.254) — `Rep` major premise
  (`zInd` / sub-`zK`), reduced by REPLACE with its own reduct.
- **`descent_step_K_noncrit_axMajor`** (`Crux2Blueprint:2174`, tag 5/6, §14.253 principal case) — L-axiom
  major premise (`zAxAll`/`zAxNeg`), reduced by the principal CUT vs the upstream R-partner (pinned by
  `majorPrem_*_cutPartner`).

This dissolves the lap-129 `permIdx` atom/`Ax¹` STALL in the live path (the major premise is provably never
a leaf) and aligns the obligation with Buchholz's actual two-case reduction. **Live-path sorries now:**
`descent_step_K_noncrit_repMajor` (:2163) + `descent_step_K_noncrit_axMajor` (:2174) + (A)
`exists_sigma1_descending_step` (:2511). (Plus the off-path dead-`red` sorries {:1253,:1356,:1529,:1616,:1705}
and the M2-side `:1867` — all unchanged, headline doesn't depend on them.)

## ⚠️ HONEST: no src sorry DROPPED — this is a sharpening, not the operator's bar
The feasibility pass (verified in-kernel) shows BOTH sub-cases bottom out in the GENERAL `Γ→⊥` Z-derivation
reduction — the genuine multi-lap content the ⊥-orbit special cases (`descent_step_Ind`/`_K_critical`) dodged:
- **repMajor**: the major premise derives `Γₘ→⊥` with `Γₘ ⊆ {A₀…A_{m−1}}` (`isChainInf` threading), possibly
  NONEMPTY. The lap-145/146 Ind soundness + the lap-142/144 critical soundness BOTH needed `Γ = ∅` (telescope
  / antecedent collapse). So this is the general reduction, not a ⊥-orbit special case.
- **axMajor**: the critical machinery does NOT extend for free — `redZKReady_of_zKValid` /
  `chainInf_redexI_data` find the redex via `inference_critical_pair_of_chain`, which CONSUMES criticality
  (`hnperm`), absent in the non-critical case; and the cut-partner `i′` need only SUCCEED `^∀p`, not be a
  direct R-intro (it can be a chain), so no redex pair need exist until `i′` is reduced.
- Confirmed `inegF p ≠ ^⊥` always (`inegF_ne_falsum`) ⟹ on `∅→⊥` a permissible premise is EXACTLY an
  `isymRep` node (`zInd`/`zK`/`zAtom`/`zAx1`) — the lap-129 non-critical picture is exact.

## 🎯 NEXT (the genuine DROP) — generalize the descent step to `Γ→⊥`, strong-induct on `iord`
Buchholz Theorem 2.1 / Corollary 2.1 as ONE generalized lemma proved by strong `iord`-induction (each premise
has strictly smaller `iord`):

  `descent_step_general` : regular `ZDerivation` of `Γ→⊥`, NOT in endform ⟹ ∃ same-sequent reduct `d'` with
  `icmp (iord d') (iord d) = 0`.

`repMajor`/`axMajor` (hence `descent_step_K_noncritical`) then fall out: reduce the major premise by the IH
(smaller `iord`), REPLACE in the chain (`isChainInf_congr` keeps validity; `iord_descent_red_zK_replace_eq`
gives descent). Per-tag reducts of `descent_step_general`: `zK`→recursion/splice (`iord_descent_red_zK_chain_*`
banked); `zInd`→Ind unfolding (generalize `descent_step_Ind` soundness off `Γ=∅`); `zAxAll`/`zAxNeg`→principal
cut (`iRKcCrit`, with `redexI < j₀` supplied from the cut-partner redex, bypassing criticality);
`zAtom`/`zAx1`→endform / §5 atomic. DESCENT halves all banked (`RedZKDescent.lean`); the work is per-tag
SOUNDNESS at nonempty `Γ`. **SMALLER first step worth a lap:** the `redexI < j₀`-from-cut-partner-redex bound
(decouple `chainInf_redexI_data` from criticality) — linchpin for `axMajor` AND the embedding.

## ⚠️ Forbidden / do-NOT-undo
- Do NOT witness any `ZDerivesEmptyR_descent_step` branch with `red` (live path is off it).
- Do NOT revert the `majorIdx` dispatch back to the `permIdx` framing (it re-introduces the lap-129 STALL).
- Do NOT attack the off-path dead `red`-soundness sorries {:1253,:1356,:1529,:1616,:1705} AS STATED.
- Do NOT re-derive the `descent_step_Ind` / `_K_critical` ⊥-orbit collapses for nonempty `Γ` — they genuinely
  needed `Γ=∅`; the nonempty-`Γ` case is the general reduction (the new `descent_step_general`).

## 📁 Key anchors
- `descent_step_K_noncritical` dispatcher `Crux2Blueprint:2190`; sub-sorries `repMajor` :2163, `axMajor` :2174.
- Major-premise machinery (InternalZ): `majorPrem_tag_mem` :9369, `majorIdx_botOrbit_reducible` :9239,
  `majorPrem_zAxAll_cutPartner` :9301, `majorPrem_zAxNeg_cutPartner` :9329, `chainInf_redexI_data` :8520,
  `redZKReady_of_zKValid` :8425.
- Banked descent halves (`RedZKDescent.lean`): `iord_descent_red_zK_replace_eq` :363, `_splice_eq` :401,
  `iRcrit_descends_or_zInd_zK_premise` :734.
- `DIRECTION.md` CURRENT DIRECTIVE (lap-146, still binding) · `STATUS.md` lap-147 · `PENDING_WORK.md` lap-147.

---
**→ Next session: build green, headline `[propext, sorryAx, Classical.choice, Quot.sound]`. §5.2 is now
decomposed on the major-premise tag (Buchholz §14.25). Genuine DROP = the `Γ→⊥`-generalized `descent_step_general`
by strong `iord`-induction (Buchholz Thm 2.1). Smaller first step = `redexI < j₀`-from-cut-partner-redex.
NEVER `red`; NEVER revert to `permIdx`.**
