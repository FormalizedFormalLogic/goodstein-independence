# HANDOFF — lap 90 · `redSound` DECOMPOSED + faithfulness wall MAPPED (route B identified)

> Supersedes prior HANDOFF-*.md. Read order: this → `ANALYSIS-2026-06-25-lap90-red-faithful-only-for-rep.md`
> → `PENDING_WORK.md` (lap-90 box) → `STATUS.md`.
> Branch `plan`. HEAD = this commit. Build 🟢 green (`lake build GoodsteinPA`, 1325 jobs). Headline
> `peano_not_proves_goodstein = [propext, sorryAx, choice, Quot.sound]` (0 math axioms).

## What this lap did (7 green commits)
Took crux-2 `redSound` (internalized cut-elimination, `Crux2Blueprint.lean`) from one opaque `sorry`
through a full decomposition + a decisive faithfulness investigation that redirects the endgame.

**Proved (machine-checked, all in `Crux2Blueprint.lean`):**
- `redSoundGen` structural-induction skeleton (5/7 `ZPhi` cases fully discharged).
- `red_zK_rep` / `red_zK_splice` — the tag-4 dispatch recursion equations (table lookups → `red dᵢ`).
- `tp_eq_isymRep_of_zTag`, `red_rep_of_tp_isymRep`, `zTag_not_iAx_of_tp_isymRep` — `Rep`-faithfulness.
- `ZDerivation_red_zK_replace` — 5.2.2 replace VALIDITY, fully proved under `tp dᵢ = isymRep`.
- `tp_isymRep_of_emptyAnt_botSucc` — Buchholz Cor 2.1 core (permissible premise of `∅→⊥` is `Rep`).

## ⭐ THE FINDING (the lap's real value — read the analysis doc)
The lap-89 dispatch's `red` keeps the chain conclusion `Π` in EVERY branch (`fstIdx_iRK = fstIdx d`),
so it equals Buchholz `d[n]` **only when `tp(d) = Rep`**. For a chain whose minimal-permissible premise
`dᵢ` is an I-rule/axiom (`tp(dᵢ) ≠ Rep`), Buchholz 5.2.2 reduces the conclusion to `tp(dᵢ)(Π,0) ≠ Π`, so
`red` is unfaithful and `red d` is not a valid `ZDerivation`. ⟹ `redSoundGen` (∀ ZDerivation) is FALSE.

- **Route A (⊥-orbit invariant `tp = Rep`) is REFUTED**: `red` of a `∅→⊥` chain recurses into `red d₀`
  where `d₀` derives `∅→A₀` (threading forces `Γ₀=∅`, but succ `A₀ ≠ ⊥`); for `∅→A₀`, `iperm` admits an
  I-rule premise, so `d₀`'s own dispatch mis-keeps the conclusion. The ⊥-condition isn't preserved one
  level down. `tp_isymRep_of_emptyAnt_botSucc` saves only the TOP step.
- **The "skip validity, pure-ordinal descent" shortcut is RULED OUT**: the critical (5.1) descent needs a
  positive-rank redex from `inference_critical_pair`, which consumes the chain's antecedent-threading +
  formula-ranks (endsequent-dependent validity). So `zKValidF`-preservation is on the critical path.
- **The Ind residual has the SAME disease**: `red(zInd)` uses fixed `k=1` + kept conclusion `Γ→F(t)`,
  but Buchholz case 4 reduces to `Γ→F(k)` with `k = value of t` and `k` copies.

## ▶ NEXT — ROUTE B: faithfully port Def 3.2 with conclusion reduction
1. Define `tpReduce I Π n` = Buchholz `I(Π,n)` (Σ₁): `R_∀xF→Γ→F(n)`, `R_¬A→…`, `L^k_A→`left-reduction,
   `Rep→Π`. The I-rule cases reuse the `substs1` machinery already in `zIallWff`/`zIndWff`.
2. Re-point the reduct's conclusion: chain/Ind/I-rule branches produce `tpReduce (tp dᵢ) Π 0` instead of
   keeping `Π`. (Ordinal side is UNAFFECTED — `iord` is endsequent-independent, descent lemmas unchanged.)
3. Invariant = Buchholz Thm 3.4(b) verbatim: `∀ d, ZDerivation d → ZDerivation (red d) ∧
   fstIdx (red d) = tpReduce (tp d) (fstIdx d) 0` — provable by PLAIN structural induction. Specialise to
   headline at `tp d = Rep` (⊥-orbit: `tpReduce Rep Π 0 = Π`, so `ZDerivesEmpty` is preserved).
4. The lap-90 proved lemmas become route-B's `tp(dᵢ) = Rep` branch; new work = the `≠ Rep` branch.
5. Tractable independent-ish thread: none truly independent (Ind has the same disease), but the 5.2.1
   splice (`ZDerivation_red_zK_splice`, when `dᵢ` is a critical chain ⟹ `tp dᵢ = Rep`) is faithful and
   could be proved like the replace branch.

## Open leaves (the headline chain, `Crux2Blueprint.lean`)
`redSoundGen` (false in general — to be replaced by the Thm-3.4(b) form), `ZDerivation_red_zK_crit`
(5.1 R2 redex recombination), `ZDerivation_red_zK_splice` (5.2.1), the Rep-selection `sorry` in
`ZDerivation_red_zK`'s 5.2.2 branch, `zKValidF_iIndReduct_of_zInd` (Ind — needs route-B conclusion+count),
`iord_descent_red`, M2 (`foundation_bot_to_Z_empty`), M3 (`false_of_ZDerivesEmpty`). Plus the live
`Reduction.lean:66` / `Statement.lean:22` headline `sorry` (not yet wired to `Crux2Blueprint`).

## Build / verify
`lake build GoodsteinPA` 🟢 1325 jobs. `#print axioms` via `lake env lean <scratch.lean>` OUTSIDE the
tree (never append-then-checkout — see `never-git-checkout-uncommitted-edits` memory).
