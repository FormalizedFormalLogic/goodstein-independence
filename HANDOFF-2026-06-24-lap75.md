# HANDOFF — lap 75 · `ZDerivation` Δ₁-definability landed (motive prereq for `ZDerivation_zsubst`)

> Supersedes prior HANDOFF-*.md. Read order: this file → `PENDING_WORK.md` (lap-74 grind box +
> lap-73 plan) → `STATUS.md`. Branch `plan`. Build 🟢 green `lake build GoodsteinPA` = **1323 jobs**.
> Tree clean. HEAD `9d3f554`. (1 commit this lap; lap started at `986400a`.)

## 🎯 OBJECTIVE (unchanged)
Discharge `peano_not_proves_goodstein` (`src/GoodsteinPA/Statement.lean:22`) **axiom-free**. Sole math
wall = crux 2, localized to `RedSound` (`InternalZ.lean`). Ladder: rung 0.5 ✅ → **rung 1 `zsubst`**
(DEFINED lap 72, `fstIdx_zsubst` lap 73, well-formedness gap (B) closed lap 74, **CORRECTNESS
`ZDerivation_zsubst` in progress**) → rung 2 (Ind reduct) → rung 3 (K/cut reduct) → rung 4 (dispatch).

## ✅ THIS LAP (`9d3f554`)
**`ZDerivation` is now `𝚫₁`-definable** — added in `InternalZ.lean` right before `zDerivation_iff`:
`zDerivationDef := zblueprint.fixpointDefΔ₁`, then `ZDerivation_defined` (via `(zconstruction).fixpoint_definedΔ₁`,
uses the existing `StrongFinite` instance), `ZDerivation_definable`, `ZDerivation_definable'`. Mirrors
Foundation's `Theory.Derivation.defined` (`Bootstrapping/Syntax/Proof/Basic.lean:496`). This is the
**motive-definability prerequisite** for the `zDerivation_induction`-driven `ZDerivation_zsubst`: its
motive `P d := d ≤ a → ZDerivation (zsubst d a t)` is `𝚫₁` = (`d ≤ a` Δ₀) → (`ZDerivation` Δ₁ ∘ `zsubst`
Σ₁-fn, params `a t` fixed). `definability` can now discharge `hP`.

## ▶ NEXT — finish `ZDerivation_zsubst` (rung-1 step 2). Plan unchanged from lap-74 PENDING box.

### Step A — term helpers (WIP saved, see below). Put in `Zsubst.lean`, ℒₒᵣ.
Needed by the **zInd case**: `termFvSubst a t (numeral 0/1) = numeral _`, `termFvSubst a t (Sa) = Sa`
for `Sa = ^&e ^+ numeral 1` with `e ≠ a`, plus closedness `IsSemiterm ℒₒᵣ 0 Sa`.
**A near-complete WIP draft is in `scratchpad/Zsubst-wip-termhelpers.lean`** (the four lemmas
`termFvSubst_numeral` / `termFvSubst_qqAdd` / `termFvSubst_succVar` / `isSemiterm_succVar`, appended
after `fvSubst_inegF`). It builds *almost*; the remaining friction (all mechanical, do NOT re-derive
from scratch — fix these specific spots):
- **`termFvSubst_func` is a `@[simp]` lemma WITH hypotheses** (`IsFunc`, `IsUTermVec`), so unlike
  `termBShift_func` it does **not** auto-fire inside a bare `simp` (that's why the lap-74 plan's
  "mirror `numeral_bShift`" simp recipe stalls on the func/vec case). **Prove `termFvSubst_qqAdd`
  FIRST (general), then do `termFvSubst_numeral`'s succ case via `numeral_add_two` + `termFvSubst_qqAdd`**
  (not via a raw `simp`).
- **`2 = 1+1` mismatch**: `termFvSubstVec_cons` matches `(kk+1)`, won't unify with literal `2`. Cleanest
  for the `?[x,y]` vec: `congr 1` then
  `rw [show (2:V)=1+1 by simp, termFvSubstVec_cons hx hYvec, show (1:V)=0+1 by simp,
      termFvSubstVec_cons hy IsUTermVec.empty, termFvSubstVec_nil]` (the `show … by simp` proves both
  since simp normalises `1+1→2`/`0+1→1`; the inner `1`-rewrite is safe — only `termFvSubstVec a t 1`
  has a literal `1:V`). Avoid the `nth_ext'` route I tried — its `j=0`-from-`j+1<2` elimination over
  `V` (no `omega`) was the time-sink.
- **`IsSemiterm.func` is an IFF** (`Term/Basic.lean:745`) — construct with `IsSemiterm.func.mpr ⟨_, _⟩`,
  not application. Vec builders: `IsUTermVec.empty` (`L 0 0`), `IsUTermVec.adjoin`, `IsSemitermVec.doubleton`.
- vec/term facts confirmed present: `LOR_func_addIndex` (simp), `numeral_uterm`/`numeral_semiterm` (simp),
  `IsUTerm.fvar`/`IsSemiterm.fvar`, `qqAdd x y = ^func 2 addIndex ?[x,y]`, `?[x,y] = x ∷ y ∷ 0`.

### Step B — add `IsSemiterm ℒₒᵣ 0 (zIndTerm d)` conjunct to `zIndWff` (`InternalZ.lean:1360`).
Unary predicate ⟹ adding a conjunct only re-proves `zIndWffDef`/`zIndWff_defined` (the `.sigma`/`.pi`
cores get `!(isSemiterm ℒₒᵣ).sigma/.pi 0 t` for `t = zIndTerm d`; mirror the existing `IsSemiformula`
splice already in `zIndWff`), **no ZPhi-cascade churn** (lap-71 finding). Needed so the zInd conclusion
transfer `fvSubst_substs1 (v := zIndTerm d)` has its closedness hyp.

### Step C — state & prove `ZDerivation_zsubst`.
```
theorem ZDerivation_zsubst {a t : V} (ht : IsSemiterm ℒₒᵣ 0 t) :
    ∀ d, ZDerivation d → d ≤ a → ZDerivation (zsubst d a t)
```
Drive by `zDerivation_induction (P := fun d => d ≤ a → ZDerivation (zsubst d a t))` — `hP` now via
`definability` (ZDerivation definable landed this lap; `zsubst_definable` exists). 7-tag `rcases` on the
`ZPhi C d` (mirror `isNF_iotil_of_ZDerivation`'s rcases at `InternalZ.lean:~3810`), build each via
`zDerivation_iff.mpr`. Per-case recipe (full detail in lap-74 PENDING box, lines ~40-49 / ~109-114):
- **atom**: `inAnt_fvSubstSeq` (no IH/fresh).
- **zIall**: IH(d0) [`d0 < zIall ≤ a`] + `fvSubst_all` (`IsUFormula p` = `hwff.2.2.isUFormula`) +
  `fvSubst_substs1_fvar` (`e ≠ a` from `a_lt_zIall` ▸ `d ≤ a` ⟹ `e < a`) + `fvSubst_isSemiformula`.
- **zIneg**: IH + `fvSubst_inegF` + `inAnt_fvSubstSeq` + `IsUFormula.fvSubst`.
- **zInd**: 2×IH + the Step-A helpers (`termFvSubst_numeral`/`_succVar`) + `fvSubst_substs1` ×3
  (numeral 0 / Sa / zIndTerm) + Step-B closedness conjunct. eig `e = zIndEig d`, `e < d ≤ a ⟹ e ≠ a`
  (`e = π₁(π₁(zRest d))`, bound via `pi₁_le_self` + the `at_lt_zInd` family).
- **zAxAll/zAxNeg**: `IsUFormula.fvSubst` + `inAnt_fvSubstSeq`.
- **zK (hard, do LAST)**: per-premise IH via `znth_zsubstTable_eq_zsubst`; `zKValid` transfer under
  subst — needs `tp`/`iperm`/`isChainInf` subst-invariance (CHECK; likely new `tp_fvSubst`/`iperm` lemmas).
Then step 3 `iotil_zsubst = iotil`, then **rung 2** (genuine Ind reduct).

## Parallel / second front
`PA_delta1Definable` (Foundation `Incompleteness/Examples.lean:17`, still an `axiom`) — mandatory for
axiom-free. Advance when the ladder blocks.

ANTI-FRAUD: discharge `Reduction.lean:68` / `Statement.lean:22` only when `#print axioms
peano_not_proves_goodstein` is clean. Do NOT axiomatize `RedSound` or fake the reduct.
