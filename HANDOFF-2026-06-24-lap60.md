# HANDOFF — 2026-06-24 (lap 60) — crux-2 §4 order theory F2/F3/F4 COMPLETE + ω-tower; both promoted to `src/`

> **Branch** `plan` · **HEAD `46b96c0`** · tree **clean** · `lake build GoodsteinPA` **green (1320 jobs)**.
> Headline `peano_not_proves_goodstein` untouched (honest `sorry`; `#print axioms` =
> `[propext, sorryAx, choice, Quot.sound]`, **0 math axioms**, anti-fraud intact).

## ⭐⭐ This lap: all four §4 natural-sum order facts F1–F4 are now machine-checked, and `src/`-promoted
Lap 59 left F1 done in `wip/InternalNadd.lean`; this lap finished F2/F3/F4 and built the ω-tower, then
**promoted both files into `src/`** (sorry-free, axiom-clean `[propext, choice, Quot.sound]`). The order
theory Buchholz §4 (Lemma 4.1 / Thm 4.2 descent) consumes is **complete**.

**`src/GoodsteinPA/InternalNadd.lean`** (natural sum `#` = `inadd`/`insTerm`; the general `InternalONote`
order spine incl. `icmp_trans`, which `InternalONote` had deliberately avoided):
- **F1** `inadd_left_mono` — strict left-monotonicity (lap 59).
- **F2** `icmp_omega_pow_nadd_lt` — `ω^{α0} # ω^{α1} ≺ ω^{α}` when `α0,α1 ≺ α` (K^r-rule split input).
- **F3** `icmp_term_lt_omega_succ` — `ω^β·k ≺ ω^{β+1}` (Ind-rule case 4), via `self_lt_iadd_one` (`β ≺ β+1`).
- **F4** `inadd_comm` — commutativity (the NF canonical-form nut), via `insTerm_comm` (single-term
  insertions commute — full 3×3 head-position split + `icmp_trans` + tail IH) and `inadd_insTerm_comm`.
- ⚠️ FIXED: `inadd_zero_right` had its `∀ a` defaulting to `ℕ` (unannotated binder + ℕ⊧IΣ1 instances);
  pinned to `∀ a : V`. Watch for this in any `∀ a, …` lemma with no `: V`.

**`src/GoodsteinPA/InternalTower.lean`** (NEW — the C1 ω-tower `ω_n(α)` for `o(d) = ω_{dg(d)}(õ(d))`):
- `iotower α n` = internal PR on a counter (param α): `ω_0=α`, `ω_{n+1}=ω^{ω_n}=ocOadd (ω_n) 1 0`.
- **`icmp_iotower_mono`** — strict base-monotonicity `α ≺ β ⟹ ω_n α ≺ ω_n β` (Thm 4.2 SAME-degree
  descent, via `icmp_omega_pow`) + `isNF_iotower` (NF preservation).

## ⭐ UPDATE (same lap, HEAD `34a4ad5`): the FULL `o(d)` monotonicity order theory is now COMPLETE.
Cross-level step landed in `src/GoodsteinPA/InternalTower.lean` (axiom-clean): `icmp_ocExp_self`
(`ocExp c ≺ c`) → `self_lt_omega_pow` (`α ≺ ω^α`) → `icmp_iotower_lt_succ` (`ω_m α ≺ ω_{m+1} α`) →
`icmp_iotower_lt_succ_of_le` (the `dg`-DROP case `ω_m α ≼ ω_m β ⟹ ω_m α ≺ ω_{m+1} β`). Together with
`icmp_iotower_mono` (same degree), **every Thm 4.2 descent inequality is now a machine-checked order
fact**. ALL crux-2 order theory (F1–F4 + tower same-level + cross-level) is DONE. What remains is purely
the CALCULUS + the assignment wiring.

## ⭐ C0 STARTED (same lap) — `wip/InternalZ.lean` (system-Z data layer, green standalone, 0 sorries)
- **Constructor codes** `zAtom`/`zIall`/`zIneg`/`zInd`/`zK` (+`𝚺₀` graphs, subterm `<`-bounds, `fstIdx`).
  `zInd` bundles eigenvar+term `at'=⟪a,t⟫` to fit `pair₆Def` (6-component max; no `pair₇Def`/`Function₆`).
- **`zTag d = π₁(sndIdx d)`** (`𝚺₀`-definable) + recognizers `zTag_zAtom=0 … zTag_zK=4`.

**⭐ DESIGN DECISION (lap 60): the assignment functions are TOTAL, the Fixpoint is secondary.**
`idg`/`iõ`/`iord` will be **total `𝚺₁` functions on ALL codes** by course-of-values recursion (mirror
`iC`/`iomul`/`iadd` table pattern in `InternalONote.lean`), dispatching on `zTag` and reading
subderivations (each `< d` by the subterm bounds). They need NO `ZDerivation` predicate to be definable.
`ZDerivation : V → Prop` (the Fixpoint) is needed ONLY to (a) characterize which codes are derivations
and (b) define `derivesEmpty`/the `⊥`-preservation — i.e. for C2/C5, NOT for C3's descent on the functions.
So **build the functions first** (they consume F1–F4 + the tower directly), defer the Fixpoint.

## NEXT LAP (hardest-first) — toward C3 (Thm 4.2 internalized), the deep crux
The §4 ORDER theory is DONE; C0 data layer is up. Build the assignment FUNCTIONS:
1. **C1a — `idg` (degree), total `𝚺₁`**: course-of-values table (mirror `iomulTable`), step branches on
   `zTag d`: atom↦0; zIall/zIneg↦`idg`(sub); zInd↦`max(idg d0 -1, max(idg d1 -1, rk p))`; zK↦max-fold
   `max_i(idg(znth ds i) -1)` and `r`. Needs `rk` (formula rank — CHECK Foundation for `IsFormula`
   complexity; else stub) + a max-over-sequence helper. Read subderivations via per-constructor
   projections (define `zIndSub0/Sub1`, `zKseq`, etc. — `π`-chains on `d-1`).
2. **C1b — `iõ` (pre-ordinal), total `𝚺₁`**: same table shape; zInd↦`ω^{iõ d0} # ω^{iõ d1 +1}`
   (uses `inadd`/`ocOadd`/`self_lt_iadd_one`); zK↦`# `-fold of `ω^{iõ dⱼ}` over `ds` (uses `inadd`).
   THIS is where F1–F4 get consumed. zIall/zIneg↦`iõ(sub)+1` (`iadd … (ocOadd 0 1 0)`).
3. **C1c — `iord d := iotower (idg d) (iõ d)`** (the tower over the pre-ordinal). Then C3 (descent) is
   stated on `iord` and proved from F1–F4 + `icmp_iotower_mono`/`icmp_iotower_lt_succ_of_le`.
4. **C0-Fixpoint (deferred, for C2/C5)** — arithmetize system Z (`ZDerivation : V → Prop`). Mirror
   Foundation's `Theory.Derivation` (Tait calculus) but with Z's rules 2–5 + atomic (rules in the
   ord-assignment doc §3). Use `Fixpoint.Construction`. THEN C1 (`iõ`/`idg`/`iord` by recursion on it,
   `iord = iotower idg iõ`), C2 (`iR`), C3 (`ord_R_descends` from F1–F4 + tower mono — THE deep induction).
3. ALT parallel: keep banging C0 in a worktree while 1 lands.

## LOCKED untouched: `Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, `Statement.lean` headline.
Aristotle OFFLINE. WebFetch dead. Crux 1 (`seqDescent_dominated`) untouched — see lap-56 STATUS box.
