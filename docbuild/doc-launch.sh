#!/usr/bin/env bash

echo 📌 When you want the local site: docbuild/build-site.py in the g-i repo, then python3 -m
echo  http.server 8127 -d ~/src/goodstein-independence/site — dep graph at the root redirect, Lean links
echo  resolve for real.

