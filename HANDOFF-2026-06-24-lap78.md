# HANDOFF — lap 78 · FRESH-MIND REVIEW · crux-2 ladder ARCHITECTURE-BLOCKED; `PA_delta1Definable` front opened

> Supersedes prior HANDOFF-*.md. Read order: this file → `STATUS.md` (lap-78 box) →
> `ANALYSIS-2026-06-24-lap78-criticality-substitution-wall.md` → `PENDING_WORK.md` (lap-78 top).
> Branch `plan`. Build 🟢 green `lake build GoodsteinPA` = **1324 jobs**. Tree clean.
> HEAD `d101f84`. (4 commits this lap: scoping/brick, docs, succIndCode typed, succIndCode raw.)

## 🎯 OBJECTIVE (operator, unchanged)
Discharge `peano_not_proves_goodstein` (`src/GoodsteinPA/Statement.lean:22`) **axiom-free** (trust base
`[propext, Classical.choice, Quot.sound]` only). TWO headline blockers: (1) crux 2 = the `RedSound`
internalized cut-elimination; (2) `PA_delta1Definable` = Foundation's upstream `axiom 𝗣𝗔.Δ₁` that Gödel II
for `𝗣𝗔` rides.

## ⭐⭐⭐ THE FINDING THIS LAP — crux-2 rung-2 plan is impossible as stated
The lap-77 handoff said "next = freshness-predicate generalization of `ZDerivation_zsubst` for rung 2."
**That is unachievable.** `ZDerivation_zsubst` (lap 77, axiom-clean, `[propext,choice,Quot.sound]`) is
complete only in the **vacuous `d ≤ a`** form. The non-vacuous form rung 2 needs (`zsubst d1 at' i`, where
`at'` occurs in `d1`) **cannot preserve the `zKValid` criticality conjunct** (`∀i, ¬iperm (tp dᵢ) s`,
`InternalZ.lean:1204`). Criticality is a formula-*inequality* `tp dᵢ ≠ seqSucc s`; `fvSubst a t` is
**non-injective** (collapses `^&a ↦ t`), so inequalities don't survive. Two explicit counterexamples
(full detail in the ANALYSIS doc):
- **CE-1** kills the lap-77 `aNotEigen d ∧ a∉FV(global concl)` plan: an *inner* chain whose conclusion
  `s'` contains `^&a` collapses (`^∀(^&a=^&a)` vs `^∀(0=^&a)` → both `^∀(0=0)` under `a↦0`).
- **CE-2** kills even **full Buchholz regularity**: a substituted numeral `i` coinciding with a conclusion
  term `F(i)` collapses (`F(^&a)` vs `F(2)` → both `F(2)` under `a↦2`). Rung 2 substitutes the whole range
  `i=0…k-1`, so any inner chain concluding `F(j)`, `j<k`, is hit.

⟹ The **chain-rule criticality design** is the wall, not a missing hypothesis. Fix = a DEEP-REFLECTION
decision (do NOT snap-pick on a grind lap): (1) re-reduction semantics; **(2) structural criticality —
track the principal premise by index/rank, not syntactic inequality vs the end-sequent (RECOMMENDED;
matches Buchholz operator-control; largest rewrite of `zKValid`/`isChainInf`/the L3.1 redex finder)**; (3)
restrict + discharge a side-condition (cheapest, likely false). **Do not resume the rung-1→2 grind until
this is decided.** `ZDerivation_zsubst` (`d ≤ a`) stays banked + axiom-clean.

## ✅ THIS LAP — pivoted to the mandatory 2nd front `PA_delta1Definable`
New file `src/GoodsteinPA/PADelta1.lean` (in the build; green 1324):
- **`paMinusDelta1 : 𝗣𝗔⁻.Δ₁` — axiom-clean** `[propext,choice,Quot.sound]`. `𝗣𝗔⁻` is finite
  (`PeanoMinus.finite`), so `Theory.Δ₁.ofFinite` enumerates it. A genuine reusable asset.
- **`paDelta1 : 𝗣𝗔.Δ₁`** assembled via `paMinusDelta1.add inductionSchemeUnivDelta1` (the defeq
  `𝗣𝗔 = 𝗣𝗔⁻ + InductionScheme ℒₒᵣ Set.univ` is `rfl`). Verified it rewires Gödel II:
  `@consistent_unprovable 𝗣𝗔 paDelta1 _ _ : 𝗣𝗔 ⊬ ↑𝗣𝗔.consistent` typechecks.
- **`inductionSchemeUnivDelta1 : (InductionScheme ℒₒᵣ Set.univ).Δ₁`** = THE remaining wall (one disclosed
  `sorry`). Foundation has NO scheme-Δ₁ infra (even `ISigma1_delta1Definable` is an upstream axiom).
  **BRICK 1 ✅ DONE THIS LAP — the internal `succInd` builder** (`src/PADelta1.lean`, all axiom-clean):
  `succIndCodeT` + `succIndCodeT_quote` (`= ⌜succInd φ⌝`); `succIndCodeRaw : V → V` (simp-normal: identity
  substs `p/[#0] = p`) + `succIndCodeT_val` (typed↔raw bridge) + `succIndCodeRaw_quote` +
  `succIndCodeRaw_definable : 𝚺₁-Function₁` (via `definability`).
  **BRICK 2 (remaining) = internal `univCl` closure `closeAll`** → `ch(y) := ∃p ≤ y, IsSemiformula 1 p ∧
  y = closeAll (succIndCodeRaw p)` (bounded ∃ ⟹ Δ₁) → `mem_iff` (via `succIndCodeRaw_quote` + a
  `closeAll`-quote lemma) → `isDelta1`. Substantial but **math-free** arithmetization.

NOT wired into `Reduction.lean` (anti-fraud — wiring now just swaps `PA_delta1Definable` for `sorryAx`).

## ▶ NEXT — two fronts, pick per how-to-get-unblocked
**(A) `PA_delta1Definable` — `inductionSchemeUnivDelta1` (RECOMMENDED, tractable, no design fork).**
Brick 1 (succIndCode) DONE + axiom-clean. **Next = BRICK 2, internal `univCl` closure `closeAll`.** No
existing Foundation machinery: build internal `fvSup` (Σ₁-recursion: max `^&i` index + 1) + the ∀-wrap.
KEY SHORTCUT: `univCl(succInd φ) = ∀^m (fixitr·succInd φ)` and `succInd φ`'s top connective is `🡒` (NOT
`∀`), so the recognizer can peel ALL leading `^∀`s of `y` to recover `m` + the matrix — exploit this to
avoid re-deriving the `fixitr` inverse. Then `ch` + `mem_iff` (`closeAll (⌜ψ⌝) = ⌜univCl ψ⌝`) + `isDelta1`.
Drops `PA_delta1Definable` from the headline (rewire `peano_not_proves_consistency` to
`@consistent_unprovable 𝗣𝗔 paDelta1`).

**(B) crux-2 criticality redesign — DEEP-REFLECTION lap, not a grind.** Decide the fork above (recommend
option 2). This unblocks rung 2 and the whole RedSound ladder. Only attempt with fresh context + the
ANALYSIS doc; it touches `zKValid`/`isChainInf`/the L3.1 redex finder across `InternalZ.lean`.

ANTI-FRAUD: discharge `Reduction.lean:68` / `Statement.lean:22` only when `#print axioms
peano_not_proves_goodstein` is clean. Do NOT axiomatize `RedSound`/`zKValid`/the scheme or fake any reduct.
