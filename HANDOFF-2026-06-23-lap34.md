# HANDOFF — 2026-06-23 (lap 34, **wall-C/D model-internal toolkit + descent scaffold**)

> **Branch** `plan` · HEAD = `392831c` · build **green** (`lake build GoodsteinPA`, **1304 jobs**) ·
> headline `Statement.peano_not_proves_goodstein` = honest `sorry` (anti-fraud intact). **Tree clean.**

Durable overview = **`STATUS.md`**. The lone deep wall is unchanged:
**`DescentSemantic.no_min_descent_absurd_of_goodstein`** (Rathjen §3 inside a model `M ⊧ paLX`), split
into `hCD` (wall C+D, the descent construction + non-termination) and `hB` (wall B, opaque
`codeOfREPred` bridge — literature-gated, `ON-LINE-REQUEST.md`). This lap drove **wall C+D**.

## Lap-34 deliverables (6 green commits; `src/` invariant held, axiom-clean)

All new lemmas in `src/GoodsteinPA/DescentSemantic.lean`, all `[propext, choice, Quot.sound]` (or +the
pre-existing `native_decide` artifact via `prec`/Seam). The **full model-internal toolkit for wall C+D**:

1. **`5255e5b`** — `lxDef_ballLT` (`fun x ↦ ∀ y<x, P y` is LX-definable when `P` is; installs
   `Structure.LT LX M` off `reductORing`, formula `(φ ⇜ ![#0]).ballLT #0`), **`lx_order_induction`**
   (`<`-below progressivity ⟹ totality, mirrors Foundation `InductionOnHierarchy.order_induction`),
   **`lx_least_number`** (every nonempty LX-definable `P` has a `<`-least witness — the choice-free
   M-internal selector). All derived from lap-33 `lx_succ_induction`.
2. **`192b1da`** — **`lx_nonterminating`** (wall-D run side, **X-essential**): given LX-definable bound
   predicate `P k := b k ≤ igoodstein m₀ k`, seed domination `b 0 ≤ m₀`, the internalized step, and
   `0<b k`, the run never reaches `0`. The iteration is `lx_succ_induction` — the corrected substrate,
   since the lap-29 `igoodstein_nonterminating_of_dominating` wants an `ℒₒᵣ`-`𝚺₁` bound but the Rathjen §3
   bound `b k = T̂^{k+2}(βₖ)` is **X-dependent** (an LX-formula).
3. **`c50d79c`** — `MX_lxDef` (MX is the `Xsym`-atom), `descentQ_lxDef` (step predicate
   `Q y := Mlt f y a ∧ ¬MX y` LX-definable, parametrizing `a` via a free var; `Mlt = prec ⇜ ![#0,&0]`
   using fvar-free `prec` + `eval_iff_of_funEqOn`), **`descent_step`** (canonical `<`-least non-`MX`
   element `Mlt`-below `a`, = `lx_least_number` on `descentQ_lxDef`).
4. **`a6ddb53`** — `lxDef_of_reduct` (predicate `ℒₒᵣ`-definable on `M`'s reduct ⟹ LX-definable via
   `lMap Φ` + `Semiformula.eval_lMap`; carries the whole `Seq`/`znth`/`igoodstein`/`T̂` substrate into
   LX-definability), `lxDef_and` (LX-definability closed under ∧, free-assignment merge via even/odd
   `Rew.rewriteMap`).
5. **`392831c`** — **`wip/DescentConstruction.lean`** (off the build, `src/` stays sorry-free): the
   `Seq`-coded M-internal descent. `IsDescent f a₀ W`; **`descent_base`/`descent_extend` PROVEN** (length-1
   base + canonical one-step `seqCons` extension, all `znth`/`¬MX`/descent clauses discharged via
   Foundation `PeanoMinus` order lemmas — **not** `omega`/`ring`, which don't work over generic `M`);
   `descent_seq_exists` (`∀k ∃W, IsDescent W ∧ lh W=k+1`, `lx_succ_induction` base/step wired). **Lone
   disclosed `sorry` = `hDdef`** (the LX-definability of `D(k) := ∃W, IsDescent f a₀ W ∧ lh W=k+1`).

## NEXT — wall C (continue from the scaffold)
1. **Discharge `hDdef`** (`wip/DescentConstruction.lean`): build the LX-formula for
   `D(k) := ∃W, IsDescent f a₀ W ∧ lh W=k+1`. It's a `Seq`-existential with `Mlt`/`¬MX` atoms on
   `znth`-terms. Plan: `lxDef_of_reduct` carries the `ℒₒᵣ` `Seq`/`lh`/`znth` graph; the `znth`-term
   X-atoms = `Xat (Φ-image of the znth-term)` built directly; assemble under one `∃W` (a bounded/`Seq`
   existential — `znth W i < W`, so it can be a bounded ∃). Then `descent_seq_exists` is sorry-free →
   promote the file to `src/`.
2. **Slow-down `βₖ`** (Rathjen 3.3/3.4/Thm 3.5): from the descent `(znth W k)` build `βₖ` with
   `C(βₖ)≤k+1`, as an LX-definable function. Substrate is in `DescentCore.lean` (route-neutral ONote/ℕ:
   `omegaStack`, `C_betaTail_le`, `repr_betaTail_*`, **`ineq6_step`**, `lemma36_nonterminating`) — the
   §3 *consumer* side is built; what's missing is the construction of `βₖ` from an arbitrary descent +
   internalization to `M`.
3. **Define `b k = T̂^{k+2}(βₖ)`, `m₀ = T̂²(β₀)`; prove `(hPdef, base, step, hpos)`** → feed
   `lx_nonterminating` ⟹ `hCD`. `step` = internalized `DescentCore.ineq6_step`.

Wall B (`hB`) stays literature-gated + independent (`ON-LINE-REQUEST.md`).

## LOCKED untouched (anti-fraud)
`Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, headline `Statement.lean` `sorry`.

## src/ code sorries (3, unchanged)
`Statement.lean:22` (headline, locked), `Reduction.lean:52` (Route-A hook, off path),
`DescentSemantic.no_min_descent_absurd_of_goodstein` (**THE wall**, disclosed `hCD`+`hB` at 410/419).

## Aristotle
Idle. Wall B is the next self-contained candidate once isolated (the `codeOfREPred`↔`igoodstein` IΣ₁
equivalence); wall C's `hDdef` is a bounded definability formula better done locally.
