/-
# Hardy & fast-growing hierarchy over `ONote` (aggregator)

Re-exports the fast-growing (`fastGrowing`, `fastGrowingε₀`) and Hardy (`hardy`) hierarchy
development. Pure `Mathlib` results (destined for upstream); see `GoodsteinPA/ToMathlib`.
-/
module

public import GoodsteinPA.ToMathlib.Hardy.VsFastGrowing
