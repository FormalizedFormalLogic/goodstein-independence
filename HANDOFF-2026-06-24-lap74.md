# HANDOFF — lap 74 · DEEP REFLECTION (direction KEPT + re-validated)

> Thin pointer. Read in order: `REFLECTION-2026-06-24-lap74.md` (this lap's primary deliverable) →
> `STATUS.md` (refreshed: header, Where-it-stands, ledger, lap-74 box) → `PENDING_WORK.md` (top:
> lap-74 reflection box, then the lap-73 `ZDerivation_zsubst` plan).
> Branch: `plan`. Build: 🟢 green, `lake build GoodsteinPA` = **1323 jobs**. HEAD at lap start `ab772d0`.

## 🎯 OBJECTIVE (unchanged)
Discharge `peano_not_proves_goodstein` (`src/GoodsteinPA/Statement.lean:22`) **axiom-free**. Sole math
wall = crux 2, localized to **`RedSound`** (`InternalZ.lean`): genuine internalized cut-elimination, via
the Option-A genuine validity-preserving reduct. Ladder: rung 0.5 ✅ → **rung 1 `zsubst`** (DEFINED lap 72,
`fstIdx_zsubst` + subst-commutation substrate lap 73) → rung 2 (Ind reduct) → rung 3 (K/cut reduct) →
rung 4 (`RedSound` tag-dispatch).

## ✅ THIS LAP — reflection synthesis (no proof code; altitude was the work)
Kernel re-verified (real `#print axioms`, green 1323): headline `[propext, sorryAx, choice, Quot.sound]`
(0 math axioms, anti-fraud intact); statement re-audited faithful (no transcription drift). **Direction
KEEP.** Three sharpenings (full detail in the reflection): (1) **PROVED crux 2 is unavoidable** — the
free-X monument is the wrong shape, the specific-instance route still needs Gentzen; do not resurrect it.
(2) **lap-70 reduct re-work** — `iR2`/`iCritReduct` definition + `iord_iR2_iterate_descends` assembly are
SUPERSEDED (stop extending `iR2`); the `iord`/ω-tower *assignment* machinery is reusable. (3)
**`PA_delta1Definable` named the second front** — still an upstream `axiom`, mandatory, untouched.
Refreshed STATUS (trimmed What's-happened to ~12) + PENDING (lap-74 box).

## ▶ NEXT (re-endorsed, unchanged from lap 73)
Finish **`ZDerivation_zsubst`** (rung-1 step 2) — the eigenvariable-substitution correctness lemma. Plan
in `PENDING_WORK.md` lap-73 box: (A) freshness via the `d ≤ a` code-bound; (B) close the well-formedness
gap by adding `IsSemiformula ℒₒᵣ 1 p` to `zIallWff`/`zIndWff` and `IsUFormula ℒₒᵣ p` to `zInegWff` (lap-71
cascade recipe; start `zInegWff`). Then step 3 `iotil_zsubst = iotil`, then rung 2 (genuine Ind reduct).

**Second front (when the ladder blocks):** `PA_delta1Definable` (Foundation `Incompleteness/Examples.lean:17`).

ANTI-FRAUD: discharge `Reduction.lean:68` / `Statement.lean:22` only when `#print axioms
peano_not_proves_goodstein` is clean. Do NOT axiomatize `RedSound` or fake the reduct.
